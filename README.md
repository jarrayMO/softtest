##### Requirements checke docker is installed in your machine
####in your terminal 

`git clone https://github.com/jarrayMO/softtest.git `

`cd softtest`

`docker-compose up -d`

`docker-compose exec app bash`

`php bin/console doctrine:migrations:migrate`
